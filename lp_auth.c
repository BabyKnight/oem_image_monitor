#include <stdio.h>
#include <string.h>

static const char* username = "";
static const char* password = "";

const char* get_username() {
		return username;
}

const char* get_password() {
		return password;
}
